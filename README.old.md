# hard-coded-React-Content-Management-System-CMS-
self-contained React application for a hard-coded Content Management System (CMS). This app uses state to manage and display different pages, with all the content written directly into the components, as requested. It's a great starting point for a simple static website or a blog that doesn't need a backend database.
